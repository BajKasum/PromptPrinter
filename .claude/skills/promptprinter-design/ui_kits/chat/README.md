# App — Chat Conversation

One chat thread: user/assistant bubbles, a typing indicator (DolphinLoader), the first-class Ergebnis (result) panel with its own copy-to-clipboard prompt block, and the sticky auto-growing composer.

Source: `src/components/app/chat-transcript.tsx`, `chat-result-panel.tsx`, `chat-composer.tsx`, `chat-empty-state.tsx`.

Composes: Button, Logo, Mascot, DolphinLoader, Icon.
