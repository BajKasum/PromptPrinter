Skeleton — pulsing loading placeholders. Compose bare `Skeleton` blocks into any layout, or use the two ready-made shapes that mirror real cards pixel-for-pixel so loading never "jumps" on arrival.

```jsx
<Skeleton style={{ height: 16, width: 120 }} />
<StatCardSkeleton />
<ProjectCardSkeleton />
```
